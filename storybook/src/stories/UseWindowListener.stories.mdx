import { Meta, Source } from '@storybook/addon-docs'
import TYPES from '../../../too-many-hooks/dist/useWindowListener.d.ts?raw'

<Meta title="Example/UseWindowListener" />

# useWindowListener

Handles adding and removing a listener on the window in a `useEffect`

## Types

<Source language="ts" code={TYPES} />
